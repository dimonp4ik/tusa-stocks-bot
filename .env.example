# Copy this file to .env and fill in your real values.
# NEVER commit the .env file to GitHub.

TELEGRAM_TOKEN=123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_CHAT_ID=your_chat_id_here

# Cosmetic only — actual scan cadence is a hardcoded 5-min cron in main.py.
# This value only feeds status-text messages ("Скан каждые X мин").
SCAN_INTERVAL_MINUTES=5
VENUE_MAX_SIGNALS_PER_SCAN=3

# Safe shadow deployment: signals are published, exchange orders are disabled.
DEPLOYMENT_MODE=shadow
STOCK_VENUE_FILTER_MODE=paper
STOCK_VENUE_FILTER_PROFILE=robust_dynamic_entry_filtered
AUTOTRADE_ENABLED=0
LIVE_HIST_EPOCH_TS=1789516800

# Autotrading: Fernet key for encrypting user OKX API keys at rest.
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AUTOTRADE_ENC_KEY=
# Optional overrides
# AUTOTRADE_ENABLED=1
# AUTOTRADE_LEVERAGE=10
# AUTOTRADE_BALANCE_THRESHOLD=100
# AUTOTRADE_CONTACT=@sanja_tusagang
# OKX_TRADE_BASE_URL=https://my.okx.com
